[build]
builder = "NIXPACKS"
buildCommand = "bash build.sh"

[deploy]
startCommand = "python manage.py migrate && gunicorn ns2_infotech_backend.wsgi"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
