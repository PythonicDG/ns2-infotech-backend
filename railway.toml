[build]
builder = "NIXPACKS"
buildCommand = "bash build.sh"

[deploy]
startCommand = "python manage.py migrate && gunicorn mia_backend.wsgi"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
